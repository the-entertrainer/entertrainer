# Asset licences and provenance

Every third-party file that ships in this site, with where it came from and
under what terms. The rule this table exists to enforce: **a "free download"
label is not a licence.** If an asset's terms cannot be established, it is
replaced with something original rather than shipped on a guess.

Last reviewed: **15 August 2026**.

---

## Typefaces

All five families are open-source under the **SIL Open Font License 1.1**,
which permits commercial use, embedding and redistribution. The full licence
text for each is bundled in `docs/licenses/`, as the OFL requires. Files were
fetched from `fonts.gstatic.com` on 15 August 2026, subset to the `latin` and
`latin-ext` ranges as Google serves them, and are otherwise **unmodified**.

| Family | Role on the site | Local files | Copyright | Licence | Licence text |
|---|---|---|---|---|---|
| Bangers | Display — nameplate, headlines, card titles | `public/fonts/webfonts/bangers-400-*.woff2` | 2010 The Bangers Project Authors | SIL OFL 1.1 | `docs/licenses/OFL-bangers.txt` |
| Space Grotesk | Interface — navigation, buttons, labels, tool UI | `public/fonts/webfonts/space-grotesk-var-*.woff2` | 2020 The Space Grotesk Project Authors | SIL OFL 1.1 | `docs/licenses/OFL-spacegrotesk.txt` |
| Source Serif 4 | Reading — decks, body copy, captions | `public/fonts/webfonts/source-serif-4-var-*.woff2` | 2014 The Source Serif 4 Project Authors (Adobe) | SIL OFL 1.1 | `docs/licenses/OFL-sourceserif4.txt` |
| IBM Plex Mono | Metadata — categories, counts, stamps, indices | `public/fonts/webfonts/ibm-plex-mono-*.woff2` | © 2017 IBM Corp., reserved font name "Plex" | SIL OFL 1.1 | `docs/licenses/OFL-ibmplexmono.txt` |
| Kalam | The hand — margin notes, pinned asides | `public/fonts/webfonts/kalam-*.woff2` | 2014 Indian Type Foundry | SIL OFL 1.1 | `docs/licenses/OFL-kalam.txt` |

**Attribution required?** No, the OFL does not require visible attribution.
The names are printed in the site footer and on `/colophon` anyway, because a
publication that argues for showing its workings should show them.

**Reserved font names.** IBM Plex reserves the name "Plex". These files are
unmodified, so nothing here triggers the renaming clause — but any future
subsetting, hinting or instancing of Plex must be published under a different
name.

One remote font remains: the Strong module (`/my-work/strong`) loads **Inter**
from Google Fonts for its own art direction. Inter is also SIL OFL 1.1
(© 2016 The Inter Project Authors). It is the only third-party request the
site makes for type.

---

## Marks and icons

| Asset | Description | Origin | Terms |
|---|---|---|---|
| `components/ed/Wordmark.vue` | The Entertrainer emblem — a ticket with two punch notches carrying an E whose middle bar is a play triangle | Original vector geometry drawn for this site | Project-owned |
| `public/favicon.svg` | The same emblem, as a standalone icon | Original, same geometry as the component | Project-owned |
| `public/favicon.ico`, `favicon-16x16.png`, `favicon-32x32.png`, `apple-touch-icon.png`, `pwa-192.png`, `pwa-512.png` | Rendered from `favicon.svg` by `@resvg/resvg-js` | Generated from the original | Project-owned |
| `public/og-card.png` | Social share card, 2400×1260 | Generated by `scripts/build-og-card.mjs` from site tokens | Project-owned |
| `public/work/ai-course-cover.png` | Cover artwork for the AI course, 1672×941 | Generated by `scripts/build-course-cover.mjs` from site tokens | Project-owned |
| Interface icons (inline SVG in components) | Chevrons, arrows, close, sun/moon, external-link | Drawn inline from simple geometry; no icon library is bundled | Project-owned |

No third-party icon set is installed. If one is ever added — Lucide and
Heroicons are both permissive and would be reasonable choices — its licence
file must be committed alongside it and listed here.

---

## Photography and artwork

| Asset | Description | Origin | Terms |
|---|---|---|---|
| `public/work/sewa/*.webp` | Pages from The SEWA Chronicles | Written, illustrated and laid out by Naveen Jose for Club Mahindra | Author's own work; shown here as portfolio evidence |
| `public/about/*.webp` | Career photographs used in the About chapters | Personal photographs | Author's own |
| `public/naveen.jpeg`, `public/about-me.png` | Portrait and About card artwork | Author's own | Project-owned |
| `public/instructional-design.png`, `work-01.png`, `strong.png`, `storygen.png`, `training-cal-gen.png`, `easymcq.png`, `better-emails.png` | Card artwork | Generated for this project, with the type set on top afterwards | Project-owned |

Any photograph that includes an identifiable third party is published with
that person's knowledge. If that is ever in doubt for a specific image, the
image comes down — that is a consent question, not a licensing one.

---

## Course media (AI course)

The AI course links out to 31 YouTube videos and 27 web resources. **Nothing is
redistributed** — no video is downloaded, mirrored or re-hosted, and the player
is a facade that contacts YouTube only when a learner presses play, via
`youtube-nocookie.com`. So the question here is verification rather than
licensing, and the register is `content/course/ai/media.ts`.

**Verified on 15 August 2026:** every video id was resolved against YouTube's
own search and oEmbed endpoints. The title, channel and runtime recorded for
each are the values YouTube returned, not values written from memory. Every
resource URL was requested and returned HTTP 200.

**Not verified, and therefore not published:** exact upload dates and caption
availability. The endpoint carrying them is blocked to automated access, and
inventing plausible dates would have been worse than omitting them. The data
model has fields for both; a maintainer with a YouTube Data API key can fill
them in programmatically.

Three URLs returned HTTP 403 to automated requests — `academic.oup.com`,
`dl.acm.org` and `openai.com` — which is bot filtering rather than a dead link.
Rather than cite links that could not be confirmed, each was replaced with a
verified alternative (a university-hosted copy of the Turing paper, and
`model-spec.openai.com`).

---

## Code dependencies

Runtime dependencies are declared in `package.json` and carry their own
licences in `node_modules`. Nothing in this repository vendors third-party
source.

## Adding an asset

Record, in this file, before it ships:

1. Local path
2. What it is
3. Creator
4. Source URL
5. The exact licence name and version
6. Whether attribution is required
7. Date checked
8. Any modification made

If any row cannot be filled in with certainty, do not ship the asset — draw
or generate a replacement. An asset that merely *resembles* a public-domain
reference is not itself public domain: the licence belongs to the file, not
to the visual idea.
